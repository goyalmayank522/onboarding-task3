<h1><%= @article.title %></h1>
<p><%= @article.body %> </p>
<ul>
  <li><%= link_to "Edit this Article", edit_article_path(@article) %></li>
  <li><%= link_to "Delete this Article", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %></li>
</ul>
